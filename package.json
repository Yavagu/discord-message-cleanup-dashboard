{
  "name": "discord-message-cleanup-dashboard",
  "version": "2.0.0",
  "description": "Secure, rate-limited, and timezone-accurate Discord message cleanup and purge dashboard.",
  "license": "GPL-3.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/Yavagu/discord-message-cleanup-dashboard.git"
  },
  "scripts": {
    "dev": "concurrently \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev",
    "build": "npm --prefix server run build && npm --prefix client run build",
    "start": "npm --prefix server run start",
    "test": "npm --prefix server run test"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
